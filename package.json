{
  "name": "iron-hunt",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev":          "vite",
    "build":        "vite build",
    "build:pages":  "vite build --mode github-pages",
    "build:demo":   "vite build --mode demo",
    "preview":      "vite preview",
    "preview:demo": "vite preview --mode demo --port 4174"
  },
  "dependencies": {
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
